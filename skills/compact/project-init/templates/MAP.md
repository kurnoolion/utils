# System map

*Not yet generated. `MAP.md` is produced by the `regen-map` skill after modules are drafted in architecture phase.*

Run `/regen-map` once `src/<module>/MODULE.md` files exist to populate the module table and dependency diagram.
